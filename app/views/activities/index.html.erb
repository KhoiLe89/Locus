<h1> All the activities of PLACEHOLDER </h1>
<ul>
<% @activities.each do |activity| %>
<li><%=link_to activity.name, activity_path(activity) %></li>
<% end %>
</ul>
<%= link_to "Add an activity", new_destination_activity_path(@destination) %>
